export const metadata = {
  title: 'Product Catalog | Wardrobe AI',
  description: 'Browse curated premium fashion and add pieces to your virtual wardrobe.',
};

export default function CatalogLayout({ children }) {
  return children;
}
