NEXT_PUBLIC_API_BASE_URL=/api
BACKEND_API_URL=http://localhost:3001/api

# Python IDM-VTON — proxied in dev via Next.js /vton-api → localhost:8000
NEXT_PUBLIC_VTON_API_URL=/vton-api
VTON_BACKEND_URL=http://127.0.0.1:8000

# Comma-separated admin emails for sidebar Admin Console (or set user.role = admin from API)
NEXT_PUBLIC_ADMIN_EMAILS=
